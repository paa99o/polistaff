@extends('layouts.app', ['title' => 'Mohon Sumbangan'])

@section('content')
<div class="row justify-content-center"><div class="col-lg-8"><div class="card"><div class="card-body"><h1 class="h4 soft-panel-title">Mohon Sumbangan</h1><p class="text-muted">Amaun tidak ditetapkan pada peringkat permohonan. Bendahari akan menetapkan had berdasarkan bajet kelab, kemudian pengerusi menentukan amaun akhir.</p><form method="post" action="{{ route('donations.store') }}">@csrf<div class="row g-3"><div class="col-md-7"><label class="form-label" for="category">Jenis Sumbangan</label><select class="form-select @error('category') is-invalid @enderror" id="category" name="category" required><option value="">Pilih jenis sumbangan</option>@foreach($types as $type)<option value="{{ $type }}" @selected(old('category') === $type)>{{ $type }}</option>@endforeach</select>@include('partials.errors', ['name' => 'category'])</div><div class="col-md-5"><label class="form-label" for="request_date">Tarikh Permohonan</label><input class="form-control @error('request_date') is-invalid @enderror" id="request_date" type="date" name="request_date" value="{{ old('request_date', now()->toDateString()) }}" max="{{ now()->toDateString() }}" required>@include('partials.errors', ['name' => 'request_date'])</div><div class="col-12"><label class="form-label" for="description">Keterangan / sebab</label><textarea class="form-control @error('description') is-invalid @enderror" id="description" name="description" rows="4" placeholder="Terangkan tujuan permohonan sumbangan">{{ old('description') }}</textarea>@include('partials.errors', ['name' => 'description'])</div></div><button class="btn btn-danger mt-3" type="submit">Mohon Sumbangan</button></form></div></div></div></div>
@endsection
