@extends('layouts.public', ['title' => $item->name.' · PoliMart'])

@section('content')
<section class="public-container public-detail-hero"><a class="public-back-link" href="{{ route('polimart.index') }}"><i class="bi bi-arrow-left me-2" aria-hidden="true"></i>Kembali ke PoliMart</a><p class="public-eyebrow">Produk komuniti</p><h1>{{ $item->name }}</h1><p class="public-detail-meta">{{ $item->category }} <span>·</span> RM {{ number_format((float) $item->price, 2) }}</p></section>
<section class="public-container public-detail-section"><article class="public-detail-card public-product-detail-card">@if($item->image_path)<img src="{{ asset('storage/'.$item->image_path) }}" alt="{{ $item->name }}">@endif<h2>{{ $item->name }}</h2><p>{{ $item->description ?: 'Tiada penerangan tambahan.' }}</p><div class="public-detail-facts"><div><strong>Penjual</strong><span>{{ $item->user->name }}</span></div><div><strong>Kategori</strong><span>{{ $item->category }}</span></div><div><strong>Harga</strong><span>RM {{ number_format((float) $item->price, 2) }}</span></div><div><strong>Stok</strong><span class="{{ $item->stock > 0 ? 'text-success' : 'text-danger' }}">{{ $item->stock > 0 ? $item->stock.' unit tinggal' : 'Habis stok' }}</span></div></div>@if($item->stock > 0)<form method="post" action="{{ route('polimart.cart.add', $item) }}" class="d-flex gap-2 align-items-end"><div><label class="form-label" for="detail-quantity">Kuantiti</label><input class="form-control" id="detail-quantity" type="number" name="quantity" value="1" min="1" max="{{ min(99, $item->stock) }}"></div>@csrf<button class="btn btn-primary" type="submit"><i class="bi bi-bag-plus me-2" aria-hidden="true"></i>Tambah ke Troli</button></form>@else<div class="alert alert-secondary mb-0"><i class="bi bi-x-circle me-2" aria-hidden="true"></i>Produk ini sudah habis stok.</div>@endif<p class="small text-muted mt-3 mb-0">Anda tidak perlu mendaftar untuk membeli. Maklumat pembeli akan diminta semasa checkout.</p></article></section>
@endsection
