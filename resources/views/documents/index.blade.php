@extends('layouts.app', ['title' => 'Dokumen Ahli'])
@section('content')
<div class="row g-4"><div class="col-lg-5"><div class="card"><div class="card-body"><h1 class="h4 soft-panel-title">Upload Dokumen</h1><form method="post" action="{{ route('documents.store') }}" enctype="multipart/form-data">@csrf<div class="mb-3"><label class="form-label">Tajuk</label><input class="form-control" name="title" required></div><div class="mb-3"><label class="form-label">Jenis Dokumen</label><input class="form-control" name="document_type" placeholder="IC / Borang / Slip" required></div><div class="mb-3"><label class="form-label">Fail</label><input class="form-control" type="file" name="document" required></div><button class="btn btn-danger">Upload</button></form></div></div></div><div class="col-lg-7"><div class="card"><div class="card-body"><h2 class="h5 soft-panel-title">Dokumen Saya</h2>@forelse($documents as $document)<div class="border-bottom py-2 d-flex justify-content-between gap-3"><div><strong>{{ $document->title }}</strong><div class="small text-muted">{{ $document->document_type }} · <a href="{{ route('documents.show',$document) }}" target="_blank">View</a></div></div><form method="post" action="{{ route('documents.destroy',$document) }}">@csrf @method('delete')<button class="btn btn-sm btn-outline-secondary">Delete</button></form></div>@empty<p class="text-muted">Tiada dokumen.</p>@endforelse</div></div><div class="mt-3">{{ $documents->links() }}</div></div></div>
@endsection
