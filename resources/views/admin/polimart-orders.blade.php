@extends('layouts.app', ['title' => 'Pesanan PoliMart'])

@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="h3 mb-1">Pesanan PoliMart</h1><p class="text-muted mb-0">Semak pesanan daripada pembeli tetamu.</p></div><span class="badge">{{ $orders->total() }} pesanan</span></div>
<div class="card"><div class="table-responsive"><table class="table mobile-records align-middle mb-0"><thead><tr><th>Pesanan</th><th>Pembeli</th><th>Item</th><th>Jumlah</th><th>Status</th><th>Tarikh</th></tr></thead><tbody>@forelse($orders as $order)<tr><td data-label="Pesanan"><strong>{{ $order->order_number }}</strong></td><td data-label="Pembeli"><strong>{{ $order->customer_name }}</strong><div class="small text-muted">{{ $order->customer_email }}<br>{{ $order->customer_phone }}</div></td><td data-label="Item">@foreach($order->items as $item)<div>{{ $item['name'] }} × {{ $item['quantity'] }}</div>@endforeach</td><td data-label="Jumlah">RM {{ number_format((float) $order->total, 2) }}</td><td data-label="Status"><span class="badge">{{ ucfirst($order->status) }}</span></td><td data-label="Tarikh">{{ $order->created_at->format('d/m/Y h:i A') }}</td></tr>@empty<tr><td colspan="6" class="text-muted">Belum ada pesanan PoliMart.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $orders->links() }}</div>
@endsection
