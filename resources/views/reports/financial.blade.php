@extends('layouts.app', ['title' => 'Laporan Kewangan'])
@section('content')
<div class="d-flex justify-content-between mb-3"><h1 class="h3">Laporan Kewangan</h1><div class="no-print d-flex gap-2"><a class="btn btn-outline-danger" href="{{ route('reports.financial.pdf', request()->only('year','month')) }}">PDF</a><a class="btn btn-outline-danger" href="{{ route('reports.financial.csv') }}">CSV</a><a class="btn btn-outline-danger" href="{{ route('reports.attendance.csv') }}">Attendance CSV</a><button onclick="print()" class="btn btn-outline-secondary">Cetak</button></div></div>
<form class="row g-2 mb-3 no-print"><div class="col-auto"><input class="form-control" type="number" name="year" value="{{ $year }}"></div><div class="col-auto"><input class="form-control" type="number" name="month" min="1" max="12" value="{{ $month }}" placeholder="Bulan"></div><div class="col-auto"><button class="btn btn-danger">Jana</button></div></form>
<div class="row g-3 mb-3"><div class="col-md-4"><div class="card"><div class="card-body"><div>Pendapatan</div><h2>RM {{ number_format((float)$income,2) }}</h2></div></div></div><div class="col-md-4"><div class="card"><div class="card-body"><div>Perbelanjaan</div><h2>RM {{ number_format((float)$expenses,2) }}</h2></div></div></div><div class="col-md-4"><div class="card"><div class="card-body"><div>Baki</div><h2>RM {{ number_format((float)($income-$expenses),2) }}</h2></div></div></div></div>
<div class="card"><div class="table-responsive"><table class="table mobile-records mb-0"><tr><th>Tarikh</th><th>Jenis</th><th>Kategori</th><th>Keterangan</th><th>Jumlah</th></tr>@foreach($transactions as $transaction)<tr><td data-label="Tarikh">{{ $transaction->transaction_date->format('d/m/Y') }}</td><td data-label="Jenis">{{ $transaction->type }}</td><td data-label="Kategori">{{ $transaction->category }}</td><td data-label="Keterangan">{{ $transaction->description }}</td><td data-label="Jumlah">RM {{ number_format((float)$transaction->amount,2) }}</td></tr>@endforeach</table></div></div>
@endsection
