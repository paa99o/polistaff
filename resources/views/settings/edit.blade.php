@extends('layouts.app', ['title' => 'System Settings'])
@section('content')
<div class="row g-4"><div class="col-lg-7"><div class="card"><div class="card-body"><h1 class="h4 soft-panel-title">System Settings</h1><form method="post" action="{{ route('settings.update') }}">@csrf @method('put')<div class="row g-3"><div class="col-md-6"><label class="form-label">Club Name</label><input class="form-control" name="club_name" value="{{ $settings['club_name'] }}"></div><div class="col-md-6"><label class="form-label">Monthly Fee</label><input class="form-control" type="number" step="0.01" name="monthly_fee" value="{{ $settings['monthly_fee'] }}"></div><div class="col-md-6"><label class="form-label">Receipt Prefix</label><input class="form-control" name="receipt_prefix" value="{{ $settings['receipt_prefix'] }}"></div><div class="col-md-6"><label class="form-label">Contact Email</label><input class="form-control" type="email" name="contact_email" value="{{ $settings['contact_email'] }}"></div><div class="col-md-6"><label class="form-label">Opening Balance</label><input class="form-control" type="number" step="0.01" name="opening_balance" value="{{ $settings['opening_balance'] }}"></div></div><button class="btn btn-danger mt-3">Save Settings</button></form></div></div></div><div class="col-lg-5"><div class="card mb-4"><div class="card-body"><h2 class="h5 soft-panel-title">Monthly Fee Automation</h2><p class="text-muted">Generate one bill per active member for the selected month. Existing bills are skipped to prevent duplicates.</p><form method="post" action="{{ route('settings.monthly-fees') }}">@csrf<label class="form-label">Billing Month</label><input class="form-control mb-3" type="month" name="billing_month" value="{{ now()->format('Y-m') }}"><button class="btn btn-danger">Generate Monthly Fees</button></form></div></div><div class="card"><div class="card-body"><h2 class="h5 soft-panel-title">Backup</h2><p class="text-muted">Export system data snapshot.</p><a class="btn btn-outline-danger" href="{{ route('backup.export') }}">Download Backup</a></div></div></div></div>
@endsection
