@extends('layouts.app', ['title' => 'Penyata Yuran'])

@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-0">Penyata Yuran</h1><p class="text-muted mb-0">Ringkasan caj bulanan, bayaran dan baki tertunggak.</p></div><a class="btn btn-outline-danger" href="{{ route('payments.index') }}">Kembali</a></div>
<div class="row g-4 mb-4"><div class="col-md-4"><div class="card stat-card"><div class="card-body"><div class="stat-label">Outstanding Balance</div><div class="stat-value">RM {{ number_format((float) $balance, 2) }}</div></div></div></div><div class="col-md-4"><div class="card stat-card"><div class="card-body"><div class="stat-label">Unpaid Bills</div><div class="stat-value">{{ $bills->whereIn('status', ['unpaid','partial','overdue'])->count() }}</div></div></div></div><div class="col-md-4"><div class="card stat-card"><div class="card-body"><div class="stat-label">Recent Payments</div><div class="stat-value">{{ $payments->count() }}</div></div></div></div></div>
<div class="card mb-4"><div class="card-body"><h2 class="h5 soft-panel-title">Monthly Bills</h2><div class="table-responsive mt-3"><table class="table mobile-records align-middle mb-0"><thead><tr><th>Month</th><th>Due Date</th><th>Charge</th><th>Paid</th><th>Balance</th><th>Status</th></tr></thead><tbody>@forelse($bills as $bill)<tr><td data-label="Bulan">{{ $bill->billing_month->format('M Y') }}</td><td data-label="Tarikh Akhir">{{ $bill->due_date?->format('d/m/Y') ?? '-' }}</td><td data-label="Caj">RM {{ number_format((float) $bill->amount, 2) }}</td><td data-label="Dibayar">RM {{ number_format((float) $bill->paid_amount, 2) }}</td><td data-label="Baki">RM {{ number_format($bill->remainingAmount(), 2) }}</td><td data-label="Status"><span class="badge bg-secondary">{{ $bill->status }}</span></td></tr>@empty<tr><td colspan="6" class="text-muted">No monthly bills yet.</td></tr>@endforelse</tbody></table></div></div></div><div class="mt-3">{{ $bills->links() }}</div>
<div class="card"><div class="card-body"><h2 class="h5 soft-panel-title">Recent Payment History</h2><div class="table-responsive mt-3"><table class="table mobile-records align-middle mb-0"><thead><tr><th>Date</th><th>Amount</th><th>Allocated</th><th>Status</th><th>Receipt</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td data-label="Tarikh">{{ $payment->payment_date->format('d/m/Y') }}</td><td data-label="Jumlah">RM {{ number_format((float) $payment->amount, 2) }}</td><td data-label="Allocated">RM {{ number_format((float) $payment->allocated_amount, 2) }}</td><td data-label="Status"><span class="badge bg-secondary">{{ $payment->status }}</span></td><td data-label="Resit">@if($payment->transaction)<a href="{{ route('transactions.show', $payment->transaction) }}">{{ $payment->transaction->receipt_number }}</a>@else - @endif</td></tr>@empty<tr><td colspan="5" class="text-muted">No payment history yet.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
