@extends('layouts.app', ['title' => 'Tuntutan Perbelanjaan'])
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-0">Tuntutan Perbelanjaan</h1><p class="text-muted mb-0">Submit and review expense claims.</p></div><a class="btn btn-danger" href="{{ route('claims.create') }}">Claim Baru</a></div><div class="card mb-3"><div class="card-body"><form class="row g-2"><div class="col-md-3"><select class="form-select" name="status"><option value="">All Status</option>@foreach(['pending','treasurer_verified','approved','rejected'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-md-3"><input class="form-control" name="category" value="{{ request('category') }}" placeholder="Category"></div><div class="col-md-2"><input class="form-control" type="date" name="from" value="{{ request('from') }}"></div><div class="col-md-2"><input class="form-control" type="date" name="to" value="{{ request('to') }}"></div><div class="col-md-2"><button class="btn btn-outline-danger w-100">Filter</button></div></form></div></div><div class="card"><div class="table-responsive"><table class="table mobile-records mb-0"><tr><th>Title</th><th>User</th><th>Amount</th><th>Status</th><th>Verified By</th><th></th></tr>@forelse($claims as $claim)<tr><td data-label="Tuntutan">{{ $claim->title }}</td><td data-label="Pengguna">{{ $claim->user->name }}</td><td data-label="Jumlah">RM {{ number_format((float)$claim->amount,2) }}</td><td data-label="Status"><span class="badge bg-secondary">{{ $claim->status }}</span></td><td data-label="Disahkan Oleh">{{ $claim->treasurerVerifier->name ?? '-' }}</td><td data-label="Tindakan"><a class="btn btn-sm btn-outline-danger" href="{{ route('claims.show',$claim) }}">View</a></td></tr>@empty<tr><td colspan="6" class="text-muted">No claims.</td></tr>@endforelse</table></div></div><div class="mt-3">{{ $claims->links() }}</div>
@endsection
